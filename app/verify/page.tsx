"use client";
import { CheckCircle2, Search, ShieldAlert } from "lucide-react";
import { useState } from "react";
import { useFormation } from "../../lib/formation-context";
import { HashDisplay, ProtocolBadge } from "../../components/ui/ProtocolPrimitives";

export default function VerifyPage() { const { receipt } = useFormation(); const [query, setQuery] = useState(""); const [checked, setChecked] = useState(false); const valid = !!receipt && (!query || query === receipt.canonicalAgreementHash || query === receipt.transactionHash || query === receipt.agreementId); return <div className="page"><div className="page-heading centered"><ProtocolBadge tone="live">PROOF VERIFICATION</ProtocolBadge><h1>Verify the meaning.</h1><p>Inspect a Formation Receipt from this browser session or paste its agreement ID, canonical hash, or transaction.</p></div><section className="verify-panel"><div className="form-field"><label htmlFor="proof-query">RECEIPT ID OR HASH</label><input id="proof-query" value={query} onChange={event => { setQuery(event.target.value); setChecked(false); }} placeholder="AG-… or sha256…" /></div><button className="button primary" onClick={() => setChecked(true)}><Search size={16} /> Verify proof</button>{checked && <div className={`verify-result ${valid ? "" : "invalid"}`}>{valid ? <><CheckCircle2 size={22} className="mint" /><h2>Formation proof verified</h2><p>This value matches the active receipt and its semantic equivalence evidence.</p>{receipt && <><HashDisplay label="CANONICAL HASH" value={receipt.canonicalAgreementHash} compact /><HashDisplay label="TRANSACTION" value={receipt.transactionHash} compact /></>}</> : <><ShieldAlert size={22} className="coral" /><h2>No matching receipt</h2><p>Check the value or complete semantic consensus and matching ratification first.</p></>}</div>}</section></div>; }
