"use client";
import Link from "next/link";
import { ArrowRight, Gauge, ShieldCheck, Sparkles } from "lucide-react";
import { useFormation } from "../lib/formation-context";
import { SemanticSculpture } from "../components/semantic/SemanticSculpture";
import { ProtocolBadge, HashDisplay } from "../components/ui/ProtocolPrimitives";
import { ScrollReveal } from "../components/ambient/ScrollReveal";
export default function Home() { const { outcome, life, canonicalHash, evaluationHash, reset } = useFormation(); return <div className="page home-page"><ScrollReveal className="hero-grid"><div className="hero-copy"><ProtocolBadge tone="live">AUTONOMOUS AGREEMENT FORMATION</ProtocolBadge><h1>Two meanings.<br /><em>One agreement.</em></h1><p>Before autonomous parties move value, CLAUSUM verifies that their interpretations establish the same obligations.</p><div className="hero-actions"><Link className="button primary" href="/workspace">Open workspace <ArrowRight size={17} /></Link><Link className="button ghost" href="/consensus">View protocol <Sparkles size={16} /></Link></div><div className="hero-proof"><span><i /> deterministic checks</span><span><i /> GenLayer judgment</span><span><i /> dual ratification</span></div></div><div className="hero-visual hero-sculpture"><SemanticSculpture /><div className="visual-caption"><span>SEMANTIC AURORA</span><small>Two interpretations enter from opposite sides. A canonical meaning emerges only when the evidence agrees.</small></div></div></ScrollReveal><ScrollReveal><section className="metric-strip"><div><Gauge size={17} /><small>NETWORK</small><strong>Studio-dev · 61997</strong></div><div><ShieldCheck size={17} /><small>ACTIVE CONTRACT</small><strong>Fresh Studio Next instance</strong></div><div><Sparkles size={17} /><small>AGREEMENT STATE</small><strong>{life}</strong></div><div><small>VERDICT</small><strong className={outcome === "EQUIVALENT" ? "mint" : outcome === "MATERIAL_CONFLICT" ? "coral" : "amber"}>{outcome}</strong></div></section></ScrollReveal><ScrollReveal><section className="home-lower"><div className="section-intro"><ProtocolBadge tone="violet">THE PROTOCOL</ProtocolBadge><h2>Meaning before commitment.</h2><p>Deterministic comparison finds what can be proven. GenLayer resolves what requires judgment. Ratification locks the resulting canonical agreement.</p><Link className="text-link" href="/workspace">Enter the agreement cockpit <ArrowRight size={15} /></Link></div><div className="hash-stack"><div><small>CURRENT CANONICAL HASH</small><HashDisplay value={canonicalHash} compact /></div><div><small>EVALUATION INPUT HASH</small><HashDisplay value={evaluationHash} compact /></div><button className="button subtle" onClick={reset}>Reset demo state</button></div></section></ScrollReveal></div>; }
