"use client";
import Link from "next/link";
import { ArrowRight, RotateCcw } from "lucide-react";
import { useFormation } from "../../lib/formation-context";
import { InterpretationPanel } from "../../components/agreement/InterpretationPanel";
import { ConvergenceRail } from "../../components/semantic/ConvergenceRail";
import { HashDisplay, ProtocolBadge } from "../../components/ui/ProtocolPrimitives";
export default function WorkspacePage() { const { agreementId, stage, conflicts, evaluationHash, amend, reset, life } = useFormation(); return <div className="page workspace-page"><div className="page-heading"><div><ProtocolBadge tone="live">WORKSPACE · {agreementId}</ProtocolBadge><h1>Agreement cockpit</h1><p>Enter two independent interpretations. CLAUSUM maps divergence before any party commits value.</p></div><div className="heading-actions"><span className="state-pill">{life}</span><button className="icon-button" onClick={reset}><RotateCcw size={15} />Reset</button></div></div><section className="agreement-banner"><div><small>PROPOSED AGREEMENT</small><h2>European provider intelligence report</h2><p>Produce a competitor report covering the five largest European providers, using public sources, by Friday at 17:00.</p></div><HashDisplay label="EVALUATION INPUT" value={evaluationHash} compact /></section><section className="interpretation-stage"><InterpretationPanel party="a" /><ConvergenceRail /><InterpretationPanel party="b" /></section><section className="workspace-footer"><div><ProtocolBadge tone={conflicts.length ? "conflict" : "success"}>{conflicts.length ? `${conflicts.length} deterministic conflicts` : "Interpretations aligned"}</ProtocolBadge><p>{stage === "conflict" ? "Amend the divergent interpretation to continue toward semantic consensus." : "The interpretations are ready for validator judgment."}</p></div>{stage === "conflict" ? <button className="button primary" onClick={amend}>Apply amendment <ArrowRight size={16} /></button> : <Link className="button primary" href="/consensus">Continue to consensus <ArrowRight size={16} /></Link>}</section></div>; }
