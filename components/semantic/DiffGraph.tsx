import { ArrowRight, Check, CircleAlert } from "lucide-react";
import { useFormation } from "../../lib/formation-context";
export function DiffGraph() { const { partyAObligations, partyBObligations, conflicts } = useFormation(); return <div className="diff-graph">{Object.entries(partyAObligations).map(([key, value]) => { const right = partyBObligations[key as keyof typeof partyBObligations]; const state = conflicts.includes(key) ? "conflict" : "match"; return <div className="diff-row" key={key}><span>{key}</span><strong className="diff-value">{String(value)}</strong><div className={`diff-link ${state}`}><ArrowRight size={14} /></div><strong className="diff-value">{String(right)}</strong><span className={`diff-state ${state}`}>{state === "match" ? <><Check size={12} /> MATCH</> : <><CircleAlert size={12} /> DIFFERENCE</>}</span></div>; })}</div>; }
